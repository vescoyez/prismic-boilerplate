extends /_layouts/app.pug

block content
  .container.mx-auto.px-4
    h1= page.title
    != asHtml(page.text)
    each project in app.filter(page => page.type === 'project')
      h2= project.data.title